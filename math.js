
/*
let PI = 3.14;

function sum(elements){ 
    let total = 0;
    for(let i=0; i<elements.length; i++){
           total+=elements[i];
    }
     return(total);
}

//console.log(sum([PI,10,8]));



function findMax(elements){
    let el = elements[0];
    for(let i=1; i<elements.length; i++){
        if(elements[i] > el){
            el = elements[i];
        }
    }

    return el; 
}
//console.log(findMax([PI,10,8]));

 


function findMin(elements){
    let el = elements[0];
    for(let i=1; i<elements.length; i++){
        if(elements[i] < el){
            el = elements[i];
        }
    }

    return el; 
}
//console.log(findMin([PI,10,8]));

     

module.exports.PI = PI;
module.exports.sum = sum;
module.exports.findMax = findMax;
module.exports.findMin = findMin;


*/